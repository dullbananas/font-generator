[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
async-std = { version = "1.10" }
deku = { version = "0.13" }
shared = { version = "0.1.0", path = "../shared" }
sled = { version = "0.34" }
tide = { version = "0.16" }
